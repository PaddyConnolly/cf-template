# 🏆 Codeforces Solutions and Template

[Codeforces](https://codeforces.com/) solutions, including high-performance Rust 1.89.0 boilerplate for quick initialisation and compile.

## Current Progress

10x800s 10x900s 10x1000s 10x1100s 

## File Creation

To create copy of `solution.rs` -> `4a.rs` in the `800s` directory, and immediately open with `nvim`:

```
chmod +x init.sh
./init.sh 800 4A
```

Compile and run with:

```
chmod +x comp.sh
./comp.sh 800 4A
```
